Se llama grafo completo, $k_n$, a un grafo no dirigido cuyos nodos tienen todos grado $n-1$, porque están conectados con el resto de nodos mediante un arco.

¿Se pueden encontrar ciclos eulerianos o hamiltonianos en un grafo $k_n$ si $n$ es par y $n \geq 3$?